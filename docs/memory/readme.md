# Field Memory

If something goes wrong on the server-side, the form will remember any inputs of the user and populate all fields so that the user does not lose any data!

Try to put `baz` into the second field and submit the form! The form will not show the success message this time. Instead it renders itself again, having all fields populated with the submitted values and showing the error message below the second field.
